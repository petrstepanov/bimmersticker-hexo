<%
	// Write schema.org product metadata

	breakArrayVariationsRecursive(products);

	products.forEach(product => {
        addBannerSunStripColorVariationsImageLinks(product);
        setUniqueIdFromVariations(product);
        setExtraProductAttributes(product);
	});
%>

<% for (product of products){ %>
  <%- partial('schema/product', {product: product}) %>
<% } %>