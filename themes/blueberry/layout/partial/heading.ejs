<div class="container pb-2 mb-4">
  <div class="row justify-content-center bar">
    <% if (title) { %>
      <h2 class="col-12 col-md-10 text-center" <%- name ? 'id="' + name + '"' : '' %>>
        <%- title %>
      </h2>
    <% } %>
    <% if (subtitle) { %>
      <p class="col-12 col-md-10 text-muted text-center">
        <%- subtitle %>
      </p>
    <% } %>
  </div>
</div>
